# fly.toml app configuration file generated for defendableos-api on 2026-05-22T15:26:05-04:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'defendableos-api'
primary_region = 'iad'
kill_signal = 'SIGINT'
kill_timeout = '30s'

[build]
  dockerfile = 'Dockerfile'

[env]
  API_BASE_URL = 'https://api.defendableos.com'
  APP_ENV = 'production'
  APP_NAME = 'DefendableOS'
  ENS_LIVE_WRITES_ENABLED = 'false'
  ENS_MODE = 'mock'
  ENS_PARENT_NAME = 'defendable.eth'
  SEED_DEMO_DATA = 'true'
  UVICORN_WORKERS = '2'
  WEB_BASE_URL = 'https://defendableos.com'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/healthz'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
